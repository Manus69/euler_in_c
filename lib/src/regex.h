#ifndef REGEX_H
#define REGEX_H

//standard:
// . any char
// * zero or more
// ? zero or one
// + at least one
// ^ start
// $ end
// | or
// () subexpr
// \ escape

//any lowercase? uppercase? digit?
// [...] literal ?

//test: match function def: word letters [(] words [)] newline ? [{]
//w .+ [(] w+ [)] [\n]? [{]
//phone number ? 

#include "def.h"

#endif